<% if signed_in? %>
  <table>
    <tr>
      <td class="main">
        <h1 class="micropost">Whats up?</h1>
          <%= render "shared/micropost_form" %>
      </td>
      <td class="sidebar round">
      <%= render "shared/user_info" %>
      </td>
    </tr>
  </table>
<% else %>

  <%= link_to "Sign up now!", signup_path, :class => "signin_button round" %>
<% end -%>